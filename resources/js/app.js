/**
import './bootstrap';
import { createApp } from 'vue';
const app = createApp({});

 import ExampleComponent from './components/ExampleComponent.vue';
app.component('example-component', ExampleComponent); 

app.mount('#app');*/

//import { vue } from 'laravel-mix';

/* import { vue } from 'laravel-mix';

const TwoWayBinding = {
    data() {
        return {
            message: 'Hello, Vue !'
        }
    }
}

vue.createApp(TwoWayBinding).mount('#two-way-buileding')*/
/*
    <div id="two-way-buildig">
        <p>{{ message }}</p>
        <input v-model="message">
    </div> 
    */
/*
const TwoWayBuilding = Vue.createApp({
    data() {
        return {
            message: 'Helo, Vue !'
        }
    }
})
TwoWayBuilding.mount('#two-way-building')
*/
